# Skeleton Dialect 代码带读（2026-08-27）

> 分支：`review-20260827` ｜ 目标：**把整个 Skeleton 代码库读成你自己的**，每读完一块能用自己的话讲清楚。
> 这不是缺陷清单，是一套**自学向导**。AI 是导读，不是作者——讲解留在你自己的复述里。
> 用法：先读 §1–§2 的两张大图（数据流 + 接口契约），然后按 §6–§10 一站一站走，每站读代码 → 复述 → 预测测试 → 跑测试对照 → 更新 §5 进度表。

---

## 1. 一张图看完整个项目（数据流）

数据从上往下流，每一行箭头代表一次 IR 到另一种 IR 的翻译。方括号里是**这个 pass 的 CLI 开关名**，括号里是实现它的 `.cpp` 文件：

```
C++ 源码（__attribute__((annotate(...)))）
  │ clang -fclangir
  ▼
CIR（cir.func / cir.call / cir.for / cir.annotation）          ← clang/lib/CIR/
  │  [cir-call-to-skeleton]  手动路径                              CirCallToSkeleton.cpp
  │  [cir-loop-to-skeleton]  半自动（目前只发 warning）             CirLoopToSkeleton.cpp
  ▼
Skeleton IR（map / reduce / vector_add / yield + preference）  ← mlir/Dialect/Skeleton/
  │  [skeleton-pattern-merge]  map{addf} → vector_add              SkeletonPatternMerge.cpp
  │  [skeleton-to-linalg]      map→generic, reduce→reduce,
  │                            vector_add→add                      SkeletonToLinalg.cpp
  ▼
Linalg（op 上挂着 skeleton.preference discardable 属性）
  │  [skeleton-preference-partition]  按 preference 拆 op 进独立 func.func，标 skeleton.target
  ▼                                                                SkeletonPreferencePartition.cpp
func.func + skeleton.target（"CPU" / "GPU"）
  │  [skeleton-target-lower]   bufferize + CPU→scf.for / GPU→scf.parallel + 设备内存桥接
  │  [skeleton-finalize-memref-to-gpu]  GPU 侧 memref 生命周期 → LLVM
  ▼                                                                SkeletonTargetLower.cpp
                                                                   SkeletonFinalizeMemRefToGpu.cpp
MLIR 既有设施（scf→cf→llvm / gpu→nvvm）→ 可执行
```

**读这张图时抓住 5 个概念：**

1. **三层 IR 接力**：CIR（Clang 的）→ Skeleton（本项目新造的方言）→ Linalg（MLIR 现成的）。每两层之间正好一个 pass 做翻译。
2. **preference 是贯穿全链路的"便签"**：Skeleton 方言属性 → to-linalg 转 discardable 属性 → partition 按它拆函数 → target-lower 按 `skeleton.target` 选 CPU/GPU。一条线到底（详见 §2 契约地图）。
3. **pure_fn 机制**：op 用 `pure_fn = @name` 引用一个**命名函数**，lowering 时把那个函数的**函数体 clone 进 linalg 的 region**。这是本项目的核心设计——纯函数是叶子节点，被骨架算子引用。
4. **两套前端路径**：手动（辅助函数调用 → op）已写但**没接上**，半自动（循环注解 → op）还是 stub。这是"继续写"的主战场（§10）。
5. **每个 pass 都有一个 .mlir 测试**：测试就是最好的"输入→输出"说明书。读 pass 时对照测试读，比纯看代码快得多。

---

## 2. 一张图看懂全链路接口（谁生产谁消费）

这张表是**继续往下写**最需要的地图。它回答一个问题：每一层之间靠什么属性/符号衔接，这个属性**在哪里被生产、在哪里被消费**。凡是"有消费点、没生产点"或"生产了、没消费"的，就是断链处。

| 契约项 | 形态 | 生产点 | 消费点 |
|--------|------|--------|--------|
| `#skeleton.preference<"CPU"/"GPU">` | `PreferenceAttr` | 手写 skeleton op /（规划中）前端 annotation | `SkeletonOps.cpp:24` verifier 限取值 |
| `skeleton.preference` | discardable attr（挂在 linalg op） | `SkeletonToLinalg.cpp:97` `setDiscardableAttr` | `SkeletonPreferencePartition.cpp:63` `getDiscardableAttr` |
| `skeleton.target` | `StringAttr`（挂在 func.func） | `SkeletonPreferencePartition.cpp:122` | `SkeletonTargetLower.cpp:55` 选 CPU/GPU；`SkeletonFinalizeMemRefToGpu.cpp:50` 判断是否激活 |
| `pure_fn` | `FlatSymbolRefAttr`（指向 func.func symbol） | 手写 skeleton op /（规划中）前端 | `SkeletonOps.cpp:35` 查签名；`SkeletonToLinalg.cpp:48` clone 函数体 |
| `skeleton.op` / `skeleton.pure` | 直接属性（挂在 cir.func） | **无生产点（断链）** | `CirCallToSkeleton.cpp:50/63` 收集 |
| `annotations` | `ArrayAttr` of `cir.annotation`（挂在 cir.for/while/do） | `CIRGenStmt.cpp:90` `attachLoopAnnotations` | `CirLoopToSkeleton.cpp:45`（只发 warning） |

**这张表里藏着两处断链，读懂它们就懂了"继续写"的方向：**

1. **手动路径断在 `skeleton.op`/`skeleton.pure`**：C++ 的 `__attribute__((annotate("skeleton_op", ...)))` 经过 CIR codegen，落到 IR 上的是 `annotations = [#cir.annotation<"skeleton_op", ["map"]>]` 这个**数组属性**，而不是名为 `skeleton.op` 的**直接属性**。`CirCallToSkeleton` 只读后者 → `opDecls` 恒空 → pass 空转。要接上，缺一个「把 `cir.annotation` 翻译成 `skeleton.op`/`skeleton.pure` 直接属性」的环节。

2. **半自动路径断在「只消费不产出」**：`annotations` 已经由 `CIRGenStmt.cpp` 生产了、也落到循环 op 上了，但 `CirLoopToSkeleton` 读到之后**只发 warning 不产出 skeleton op**。这一条的生产端是通的，缺的是消费端把它变成 op 的那段分析代码。

> 一句话记住：**前端两条路径都卡在「生产 → 消费」的衔接上**。后端（Skeleton → Linalg → target）的链是完整的、能端到端跑的。

---

## 3. 文件地图（每个文件是干什么的）

**IR 层** `mlir/include/mlir/Dialect/Skeleton/IR/` + `mlir/lib/Dialect/Skeleton/IR/`

| 文件 | 角色 | 一句话 |
|------|------|--------|
| [`SkeletonDialect.td`](../../mlir/include/mlir/Dialect/Skeleton/IR/SkeletonDialect.td) | 方言定义 | 方言名 `skeleton`、命名空间、`useDefaultAttributePrinterParser` |
| [`SkeletonAttrs.td`](../../mlir/include/mlir/Dialect/Skeleton/IR/SkeletonAttrs.td) | 属性定义 | `preference` 属性（CPU/GPU），`genVerifyDecl=1` 让 verifier 手写 |
| [`SkeletonOps.td`](../../mlir/include/mlir/Dialect/Skeleton/IR/SkeletonOps.td) | op 定义（TableGen） | 4 个 op 的语法、参数、region |
| [`SkeletonDialect.cpp`](../../mlir/lib/Dialect/Skeleton/IR/SkeletonDialect.cpp) | 方言 + 属性注册 | `PreferenceAttr::verify`（限 CPU/GPU）+ 注册表 |
| [`SkeletonOps.cpp`](../../mlir/lib/Dialect/Skeleton/IR/SkeletonOps.cpp) | op 实现 | 3 个 verifier + 公共 `verifyPureFnSignature` |

**Transforms 层** `mlir/lib/Dialect/Skeleton/Transforms/`（5 个 pass，按管线顺序）

| 文件 | CLI 开关 | 做什么 |
|------|---------|--------|
| [`SkeletonPatternMerge.cpp`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonPatternMerge.cpp) | `skeleton-pattern-merge` | `map{pure_fn=@addf}` → `vector_add`（命名 op 的诞生） |
| [`SkeletonToLinalg.cpp`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonToLinalg.cpp) | `skeleton-to-linalg` | map/reduce/vector_add → linalg.generic/reduce/add |
| [`SkeletonPreferencePartition.cpp`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonPreferencePartition.cpp) | `skeleton-preference-partition` | 把带 preference 的 linalg op **outline** 进独立 func.func |
| [`SkeletonTargetLower.cpp`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonTargetLower.cpp) | `skeleton-target-lower` | 最复杂：bufferize + 按 target 生成 CPU 循环 / GPU 并行 + 设备内存桥接 |
| [`SkeletonFinalizeMemRefToGpu.cpp`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonFinalizeMemRefToGpu.cpp) | `skeleton-finalize-memref-to-gpu` | GPU 收尾：host 侧 memref 生命周期 → LLVM |

> pass 的注册与声明在 [`Passes.td`](../../mlir/include/mlir/Dialect/Skeleton/Transforms/Passes.td)。**注意**：那里每个 pass 的 `description` 还有 `custom_matmul` 时代的过时描述（如 `SkeletonToLinalg` 写「Converts skeleton.custom_matmul」），读实现别信描述，以 `.cpp` 为准。

**CIR 前端** `clang/lib/CIR/Dialect/Transforms/` + `clang/lib/CIR/CodeGen/`

| 文件 | CLI 开关 | 做什么 |
|------|---------|--------|
| [`CirCallToSkeleton.cpp`](../../clang/lib/CIR/Dialect/Transforms/CirCallToSkeleton.cpp) | `cir-call-to-skeleton` | 手动路径：识别辅助函数调用 → skeleton op（**当前空转**） |
| [`CirLoopToSkeleton.cpp`](../../clang/lib/CIR/Dialect/Transforms/CirLoopToSkeleton.cpp) | `cir-loop-to-skeleton` | 半自动路径 stub（只发 warning） |
| [`CIRGenStmt.cpp`](../../clang/lib/CIR/CodeGen/CIRGenStmt.cpp) | — | 把 C++ 注解落到 cir.for/while/do 的 `annotations` 属性 |

**测试（理解代码的入口）**

| 测试 | 验证什么 |
|------|----------|
| [`skeleton-ops-valid.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-ops-valid.mlir) | op 语法 + verifier 正向 |
| [`skeleton-ops-invalid.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-ops-invalid.mlir) | verifier 报错路径（`-verify-diagnostics`） |
| [`skeleton-pattern-merge.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-pattern-merge.mlir) | map{addf} 合并成 vector_add；mul 不合并 |
| [`skeleton-to-linalg.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-to-linalg.mlir) | skeleton → linalg 的翻译 + preference 保留 |
| [`skeleton-preference-partition.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-preference-partition.mlir) | 按 preference 拆函数（单/混合/无 preference） |
| [`skeleton-target-lower.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-target-lower.mlir) | CPU→scf.for、GPU→scf.parallel→gpu.launch_func |
| [`skeleton-finalize-memref-to-gpu.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-finalize-memref-to-gpu.mlir) | gpu.memcpy→llvm、alloc/dealloc 保留 |
| [`cir-loop-to-skeleton.cir`](../../clang/test/CIR/Transforms/cir-loop-to-skeleton.cir) | 半自动路径的 warning |

**集成测试**（端到端跑数值，`mlir/test/Integration/Dialect/Skeleton/`）

| 路径 | 验证内容 |
|------|----------|
| `CPU/test-skeleton-vector-add.mlir` | vector_add → linalg.add → JIT 数值 |
| `CPU/test-skeleton-vector-add-e2e.mlir` | 同上（e2e 入口） |
| `CPU/test-skeleton-map-add-cpu.mlir` | map{pure_fn} → linalg.generic → JIT |
| `CPU/test-skeleton-reduce-sum-cpu.mlir` | reduce{pure_fn} → linalg.reduce → JIT |
| `CPU/test-skeleton-matmul.mlir` | matmul（CPU）→ scf 循环 → JIT |
| `GPU/test-skeleton-matmul-gpu.mlir` | matmul（GPU）→ CUDA runner（需 GPU） |

> 集成测试的 `// RUN:` 行就是**完整 pipeline 命令**，比任何文档都权威——想跑通端到端，抄 `test-skeleton-matmul.mlir` 第一行的 pass 顺序即可。

---

## 4. 阅读路线（5 站）

底向上读：先把地基（IR 层）讲清楚，再一步步往下走。**先看大图（§1–§2），然后回站 1 开始。**

| 站 | 读什么 | 配套测试 | 读完能回答 |
|----|--------|---------|-----------|
| **1. IR 层** | `SkeletonOps.td` + `SkeletonOps.cpp` | valid / invalid | 4 个 op 是什么？verifier 在约束什么？ |
| **2. 第一次 lowering** | `SkeletonPatternMerge` + `SkeletonToLinalg` | pattern-merge / to-linalg | map 怎么变成 linalg.generic？pure_fn 的函数体去哪了？ |
| **3. preference 分拆** | `SkeletonPreferencePartition` | partition | 一个 linalg op 怎么被拆成独立函数？ |
| **4. target 生成（最大）** | `SkeletonTargetLower` + `SkeletonFinalizeMemRefToGpu` | target-lower + 集成测试 | CPU/GPU 代码怎么分叉？GPU 内存怎么桥接？ |
| **5. CIR 前端（最后）** | `CirCallToSkeleton` + `CirLoopToSkeleton` + `CIRGenStmt` | cir-loop-to-skeleton | 注解怎么从 C++ 一路变成 skeleton op？（手动路径断在哪） |

> **为什么站 5 排最后**：`CirCallToSkeleton.cpp` 是全项目最不完整的一块——它读的 `skeleton.pure` / `skeleton.op` 属性没有任何地方生产（这正是 §2 断链 1）。前面 4 站建立的地基，正好让你读懂"它断在哪、缺什么"。站 5 也是你「继续写」最可能落手的地方。

---

## 5. 进度

> 状态：⬜ 未读 ｜ 🟡 在读 ｜ ✅ 能讲清楚
> 完成标准 = 能用**自己的话**讲清这站代码做什么、为什么、现状边界在哪，并跑通配套测试的预测对照。

| # | 站 | 文件 | 状态 | 完成日期 | 我的复述要点（写给自己看） |
|---|----|------|------|---------|--------------------------|
| 1 | IR 层 | `SkeletonOps.td` + `SkeletonOps.cpp` | ⬜ | | |
| 2 | skeleton→linalg | `SkeletonPatternMerge.cpp` + `SkeletonToLinalg.cpp` | ⬜ | | |
| 3 | preference 分拆 | `SkeletonPreferencePartition.cpp` | ⬜ | | |
| 4 | target 生成 | `SkeletonTargetLower.cpp` + `SkeletonFinalizeMemRefToGpu.cpp` | ⬜ | | |
| 5 | CIR 前端 | `CirCallToSkeleton.cpp` + `CirLoopToSkeleton.cpp` + `CIRGenStmt.cpp` | ⬜ | | |

**全局大图自测（读完 §1–§2 后）：**

- [ ] 不看文档，能画出「C++ → CIR → Skeleton → Linalg → target」的管线图
- [ ] 能默写 §2 契约地图里的 6 个契约项，说出每个「谁生产、谁消费」
- [ ] 能指出两处断链各自卡在「生产」还是「消费」
- [ ] 知道 pure_fn 是什么、lowering 时函数体去哪了
- [ ] 知道手动路径和半自动路径的区别、各自在哪

**每站读完**，把你自己发现的边界、想不通又搞懂的点记在对应站的「你的复述」里。

---

## 6. 站 1 · IR 层（地基）

> 读完这站，你该能：**说出 4 个 op + 1 个属性各自是什么、verifier 在约束什么，并能预测一个用例是过还是挂。**

### 读这些（按顺序）

1. [`SkeletonAttrs.td`](../../mlir/include/mlir/Dialect/Skeleton/IR/SkeletonAttrs.td)（31 行，2 分钟）——`preference` 属性定义，注意 `genVerifyDecl = 1`。
2. [`SkeletonDialect.td`](../../mlir/include/mlir/Dialect/Skeleton/IR/SkeletonDialect.td)（34 行，1 分钟）——方言定义，扫一眼即可。
3. [`SkeletonDialect.cpp`](../../mlir/lib/Dialect/Skeleton/IR/SkeletonDialect.cpp)（60 行，2 分钟）——只看 `PreferenceAttr::verify`（23–30 行）和最后的注册部分（43–60 行）。
4. [`SkeletonOps.td`](../../mlir/include/mlir/Dialect/Skeleton/IR/SkeletonOps.td)（182 行）——4 个 op 的定义。**重点读 assemblyFormat（语法）和 arguments（参数）**，description 里的大段文字可略读。
5. [`SkeletonOps.cpp`](../../mlir/lib/Dialect/Skeleton/IR/SkeletonOps.cpp)（248 行）——公共 `verifyPureFnSignature`（27–72 行）+ 3 个 verifier：`MapOp::verify`（78 行）、`ReduceOp::verify`（147 行）、`VectorAddOp::verify`（214 行）。

然后对照测试：

6. [`skeleton-ops-valid.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-ops-valid.mlir)（正向用例）
7. [`skeleton-ops-invalid.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-ops-invalid.mlir)（报错用例，`-verify-diagnostics` 模式）

### 带着问题读

- **map** 有 `ins`（可变个）、`outs`（1 个）、`result`（1 个）。verifier 里 `output` 和 `result` 之间要满足什么关系？（`outputType != resultType` → 报错；这是**完整 RankedTensorType 比较**，含 rank/shape/元素类型）
- **reduce** 对输出 rank 有什么要求？（必须 rank-0 标量）对输出元素类型呢？（必须 == 输入元素类型）
- **yield** 是谁约束它只能出现在哪？（`HasParent<"MapOp, ReduceOp">`）
- **vector_add** 为什么叫 "named op"？它和 map 比，少了哪个自由度？（它把「任意纯函数」固定成了「加法」，且只能 1D）
- `verifyPureFnSignature` 从哪找 `pure_fn` 指向的函数？（`SymbolTable::lookupNearestSymbolFrom<func::FuncOp>`）它检查了 4 件事：参数个数、每个参数类型、结果个数（==1）、结果类型。
- `pure_fn` 和 `body` region 是什么关系？（二选一必填）

> **注解（2026-08-27）**：文档预期是「二选一」，但代码原实现只拦「两个都没有」：`if (!hasPureFn && !hasBody)`（[`SkeletonOps.cpp:83`](../../mlir/lib/Dialect/Skeleton/IR/SkeletonOps.cpp#L83)），所以 **hybrid（`pure_fn` + `body` 同时存在）能过验证**——而它没有语义：pattern-merge 与 to-linalg 都以 `body` 为空为匹配前提（[`SkeletonPatternMerge.cpp:73`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonPatternMerge.cpp#L73)、[`SkeletonToLinalg.cpp:41`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonToLinalg.cpp#L41)），hybrid 不匹配任何 pattern，会残留到 LLVM conversion 才报错，其 `pure_fn` 是永不读取的死属性。本次已把条件改为 `hasPureFn == hasBody`（exactly one），并补了 hybrid 反例测试。

### 读的时候注意（先自己想）

1. **文档例子 vs verifier 的矛盾**：[`SkeletonOps.td:42`](../../mlir/include/mlir/Dialect/Skeleton/IR/SkeletonOps.td#L42) 的嵌套例子，block 参数是**行切片 tensor**（`^bb0(%row: tensor<?xf32>, ...)`）。但 [`SkeletonOps.cpp:127-137`](../../mlir/lib/Dialect/Skeleton/IR/SkeletonOps.cpp#L127) 的 `MapOp::verify` 检查 block 参数类型 == **元素类型**（`f32`）。这两个对不上。→ 这是「愿景 vs 实现」的第一处裂缝：CLAUDE.md 设想的「map 内嵌 reduce 逐行归约」在当前 verifier 下**不可表达**。

2. **ReduceOp 注释 vs 代码**：[`SkeletonOps.cpp:165`](../../mlir/lib/Dialect/Skeleton/IR/SkeletonOps.cpp#L165) 的注释说「Input must be at least 1D」，但往下看代码——**只检查了输出 rank == 0，没检查输入 rank**。rank-0 输入也会通过验证。

3. **MapOp 不查 input vs output 的 rank/shape**：它查了 `outputType == resultType`，但**没查 input 和 output 之间 rank/shape 是否一致**（`tensor<2x3xf32>` 输入配 `tensor<2xf32>` 输出，能过验证）。这个不查，站 2 的 `SkeletonToLinalg` 会按 result 的 rank 生成 indexing map，然后错位。

4. **VectorAddOp 要求全 1D**：[`SkeletonOps.cpp:225`](../../mlir/lib/Dialect/Skeleton/IR/SkeletonOps.cpp#L225) 强制所有操作数 rank == 1。记下这条，站 2 的 pattern-merge 会踩它。

### 自测（讲得出来才算过）

- [ ] 不看文件，默写 4 个 op 的语法（`ins/outs -> result` 长什么样）。
- [ ] 说出 reduce 的「输出必须 rank-0」这条约束在 verifier 哪一行、为什么设计成 rank-0。
- [ ] 说出 `verifyPureFnSignature` 检查的 4 件事。
- [ ] 预测：`skeleton.map pure_fn=@f ins(%a: tensor<4xf32>) outs(%b: tensor<4xf32>) -> tensor<4xf32>`，其中 `@f` 是 `(f32) -> f32`。能过吗？跑一下确认。
- [ ] 预测：`#skeleton.preference<"TPU">` 会怎样？在 invalid 测试里找有没有这条反例（**没有**——这是个覆盖缺口）。跑一下确认。
- [ ] 跑测试：`build/bin/llvm-lit -v build/tools/mlir/test/Dialect/Skeleton/`，确认单测全绿。

### 这一站的现状与边界（继续写时要知道的）

- IR 层是**全项目最扎实的一层**：4 个 op + 1 个属性定义完整，verifier 覆盖了「缺 pure_fn/body」「output≠result」「签名不匹配」等主要约束。
- 未覆盖的边界有三处，都在 verifier 里补检查就能解决：① MapOp 不查 input/output rank 一致；② ReduceOp 不查 input rank ≥ 1（注释写了但没实现）；③ `PreferenceAttr` 的非法取值没有反例测试。
- 若你要实现「行切片 body / 嵌套 reduce」这个愿景，动的不是 verifier 一个地方——verifier、to-linalg 的 lowering、以及 pattern-merge 的 rank 假设要一起改。

> **注解（2026-08-27）**：这条列的约束此前缺一个反例——「缺 pure_fn/body」只拦「两个都没有」，**不拦「两个都有」**。hybrid 形态能过验证但无任何 lowering 消费方处理，属无语义的死 IR。已把条件改为 exactly one 并补反例，这一条才覆盖完整。

### 你的复述

（用你自己的话，把这一站讲一遍。讲完你就过关了。）

---

## 7. 站 2 · skeleton → linalg（第一次 lowering）

> 读完这站，你该能：**说出 map/reduce/vector_add 是怎么变成 linalg 的、pure_fn 的函数体在 lowering 时去了哪，以及 pattern-merge 把 map 变成 vector_add 的判定条件。**

### 读这些（按顺序）

1. [`SkeletonPatternMerge.cpp`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonPatternMerge.cpp)（127 行）——先读这个，它最小最独立。
   - `isAddFn`（37–64 行）：判定一个纯函数「是不是加法」。
   - `MergeMapAddToVectorAdd`（67–104 行）：把 `map{pure_fn=@add}` 重写成 `vector_add`。
2. [`SkeletonToLinalg.cpp`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonToLinalg.cpp)（206 行）——主 lowering。
   - `ConvertMapOp`（35–102 行）：map → `linalg.generic`。
   - `ConvertReduceOp`（108–157 行）：reduce → `linalg.reduce`。
   - `ConvertVectorAddOp`（163–178 行）：vector_add → `linalg.add`。
3. 对照测试：
   - [`skeleton-pattern-merge.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-pattern-merge.mlir)
   - [`skeleton-to-linalg.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-to-linalg.mlir)

### 带着问题读

- `isAddFn` 判定「是加法」的**全部条件**是什么？（4 个：恰好 1 个 block、恰好 2 个 op、第一个 op 是 `arith.addf` 且操作数是 block 参数、terminator 是 `return` 且返回 addf 结果）——注意它只认 `arith.addf`，`addi`（整数加）和 `mulf` 都不认。
- map → generic 时，indexing map 和 iterator types 是怎么生成的？（对每个 operand 生成 `AffineMap::getMultiDimIdentityMap(rank)`，全 `parallel`——逐元素算子，安全）
- **pure_fn 的函数体去哪了**？看 `ConvertMapOp` 的 region 构建回调（78–93 行）：`IRMapping` 把 `fn.getArgument(i)` 映射到 `blockArgs[i]`，然后 `clone` fn 的 body（去掉 terminator），最后把 `func::ReturnOp` 换成 `linalg::YieldOp`。**这就是「函数体内联」**——纯函数最后成了 linalg region 里的一段代码，而不是一次函数调用。
- preference 属性是怎么保留下来的？（`setDiscardableAttr` 97 行——用 discardable 是为了不污染 linalg 方言的 op 定义）
- pattern-merge 和 to-linalg 谁先谁后？如果两个 pass 都跑，map 会不会被重复处理？（merge 成功就把 map 替换成 vector_add 了，to-linalg 再处理 vector_add；两者是**有序衔接**，不是竞态）

### 读的时候注意（先自己想）

1. **pattern-merge 不检查 rank**（[`SkeletonPatternMerge.cpp:78-81`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonPatternMerge.cpp#L78) 有 TODO 点明）：merge 只查「输入个数=2、有 pure_fn、无 body、isAddFn」，**没查 rank**。站 1 我们记了 `VectorAddOp::verify` 要求全 1D——那一个 **2D 的** `map{pure_fn=@add}` 被 merge 成 `vector_add` 后，会**生成一个过不了验证的 IR**。

2. **函数体 clone 的单 block 假设**（[`SkeletonToLinalg.cpp:79-82`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonToLinalg.cpp#L79) + `136-138`）：clone 只取 `fn.getBody().front()`（**第一个 block**），terminator 直接 `cast<func::ReturnOp>`。如果 pure_fn 函数体里有 if/else（多 block），多出来的 block 被**静默丢弃**。map / reduce 两处同病。

3. **reduce 的 dimensions**（[`SkeletonToLinalg.cpp:134`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonToLinalg.cpp#L134)）：`dimensions = 0..inputRank-1`，即沿**所有维度归约**到 rank-0。站 1 我们确认了 ReduceOp 的 verifier 没查输入 rank ≥ 1——rank-0 输入会生成维度数组为空的 reduce，行为未定义。

4. **`isAddFn` 只认 `arith.addf`**（[`SkeletonPatternMerge.cpp:47`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonPatternMerge.cpp#L47)）——整数加法、`f64` 加法都不检查元素类型，只认 op 是 addf 就合并。

### 自测（讲得出来才算过）

- [ ] 不看文件，说出 `map{pure_fn=@add}` → `vector_add` 需要满足的全部判定条件。
- [ ] 说出 pure_fn 函数体在 map→generic 时经历了什么（映射 → clone → 换 terminator）。
- [ ] 预测：`tensor<2x3xf32>` 的 `map{pure_fn=@my_add}` 过 pattern-merge 后，`vector_add` 能通过验证吗？跑一下确认（这就是为什么会有那个 TODO）。
- [ ] 在 `skeleton-pattern-merge.mlir` 里找一个「不该合并」的用例（mul），说出它为什么不会被合并。
- [ ] 跑测试确认两个测试文件都过。

### 这一站的现状与边界（继续写时要知道的）

- 这一站是**后端里「翻译」最核心的一站**，CPU 路径从这里开始就自洽了。
- pure_fn 的「函数体内联」是本项目最巧妙的设计：纯函数最终不是被调用，而是被 clone 成 region 代码。但当前 clone **只支持单 block 直线代码**。
- 若要让纯函数支持控制流（if/else），要改的是 clone 逻辑（拒绝多 block 或正确内联），不是 op 定义。
- 若要让 pattern-merge 安全，在 merge 前查 rank 和元素类型即可（TODO 已经指明了位置）。

### 你的复述

（用你自己的话，把这一站讲一遍。讲完你就过关了。）

---

## 8. 站 3 · preference 分拆（outline）

> 读完这站，你该能：**说出一个带 `skeleton.preference` 的 linalg op 是怎么被拆进独立 func.func 的、`skeleton.target` 标在哪、原 op 被什么替换。**

### 读这些（按顺序）

1. [`SkeletonPreferencePartition.cpp`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonPreferencePartition.cpp)（164 行）——只有两个 phase，很短：
   - **Phase 1**（56–92 行）：walk 出所有带 `skeleton.preference` 的 linalg op，存进 `OutlineEntry` 结构体（35–44 行）。**注意快照了哪些东西**（`entry.inputs`、`entry.outputs`、`entry.resultTypes`）。
   - **Phase 2**（94–153 行）：对每个 entry，建新 func.func，clone op 进新函数，原位替换成 `func.call`。
2. 对照测试：[`skeleton-preference-partition.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-preference-partition.mlir)
   - 重点看 `test_mixed` 用例——它有几个 matmul、各自什么偏好、结果长什么样。

### 带着问题读

- Phase 1 收集时，靠什么判断「这个 op 要 outline」？（`getDiscardableAttr("skeleton.preference")`——这就是站 2 `setDiscardableAttr` 留下的属性，在这一站被消费）
- 新函数名怎么生成的？（`preference + "_" + opName + "_" + index`，[`SkeletonPreferencePartition.cpp:116-119`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonPreferencePartition.cpp#L116)）`opName` 从哪来？（`linalgOp->getName().stripDialect()`，即 `matmul`/`add`/`generic` 等）
- `skeleton.target` 属性标在**函数**上还是 op 上？（函数上，122 行）
- 原 op 是怎么被替换掉的？（147–152 行：建 call → `op->replaceAllUsesWith(call.getResults())` → `op->erase()`）
- 一个函数里如果有两个带偏好的 op，会生成几个新函数？（两个——每个 op 一个独立函数）

### 读的时候注意（先自己想）

1. **Phase 1 快照 vs Phase 2 实际使用的脱节（这一站最重要的边界）**：Phase 1 把 `entry.inputs/outputs` 快照成 **Value**（[`SkeletonPreferencePartition.cpp:78-79`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonPreferencePartition.cpp#L78)）。Phase 2 循环里先处理 producer，`op->replaceAllUsesWith(call.getResults())` 会把 **consumer 的操作数**从旧值替换成新 call 的结果。等到处理 consumer 时：
   - `entry.inputs` 里存的还是**旧的、已 dangling 的 Value**；
   - 而 consumer 的实际操作数已经是新 call 的结果；
   - clone 用 `mapper.map(callOperands[i], entryBlock->getArgument(i))`（131–132 行）映射**旧值 → block arg**，但 clone 出来的 op 里引用的是**新值**（在函数外部）→ 生成非法 IR。
   - **别急着背这段。** 读代码时自己走一遍：两个 matmul 链式（`%0 = matmul1(...)`, `%1 = matmul2(%0, ...)`），先处理 matmul1 会发生什么，再处理 matmul2 时 `entry.inputs` 里的 `%0` 还活着吗？
   - 现有测试 `test_mixed` 里三个 matmul 都用函数入参、互不依赖，所以没触发。**你想想：补一个链式用例会怎样？**

### 自测（讲得出来才算过）

- [ ] 不看文件，说出 outline 的 4 步：收集 → 建函数 → clone op → 替换成 call。
- [ ] 说出 `skeleton.target` 标在哪个层级、值是什么。
- [ ] 自己设计一个链式用例（两个 matmul 依赖），预测 partition 之后 IR 会不会坏，然后（如果愿意）跑一下验证。
- [ ] 跑测试确认 `skeleton-preference-partition.mlir` 通过。

### 这一站的现状与边界（继续写时要知道的）

- 这一站是**「按 preference 切分程序」的雏形**，也是 §2 契约里「`skeleton.preference` → `skeleton.target`」这一跳的落点。
- 它的正确性边界是：**只对「互不依赖的 op」安全**。链式 producer-consumer 会因快照脱节生成非法 IR。
- 若要让链式依赖正确，outline 策略要改：要么按拓扑序处理（先 producer 后 consumer 时，用 use 追踪拿到最新值），要么在 Phase 2 重建 operands 而不是用 Phase 1 的快照。

### 你的复述

（用你自己的话，把这一站讲一遍。讲完你就过关了。）

---

## 9. 站 4 · target 生成（最大的一站）

> 读完这站，你该能：**说出这个 pass 的三个阶段（bufferize → GPU 内存桥接 → 按 target 生成循环），以及 GPU 内存为什么必须桥接、桥接做了哪几件事。**

### 读这些（按顺序）

1. [`SkeletonTargetLower.cpp`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonTargetLower.cpp)（255 行）——按这个顺序读：
   - **入口**（45–70 行）：收集带 `skeleton.target` 的函数，区分 GPU 函数。
   - **Step 1**（74–81 行）：`one-shot-bufferize`，`bufferizeFunctionBoundaries = true`（为什么要函数边界模式？因为要改函数签名）。
   - **Step 1b / GPU bridge**（90–223 行）——**全文件最核心、最难的部分**：
     - 收集 GPU call 的每个 memref 操作数（99–110 行）。
     - **Phase A**（116–165 行）：每个 GPU call 之前——`gpu.alloc`（设备内存，同步）+ `gpu.memcpy`（H→D，异步）+ `memref.cast` 替换 call 操作数。
     - **Phase B**（167–222 行）：每个 GPU call 之后——`gpu.memcpy`（D→H）+ `gpu.dealloc`，把 call 结果换回 host memref。
   - **Step 2**（226–244 行）：按 target 逐函数 lower——CPU → `convert-linalg-to-loops`（scf.for），GPU → `convert-linalg-to-parallel-loops`（scf.parallel）。
2. [`SkeletonFinalizeMemRefToGpu.cpp`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonFinalizeMemRefToGpu.cpp)（102 行）——很短，看它**把什么标为 illegal / legal**（`addIllegalOp` / `addLegalOp` / `addLegalDialect`）。
3. 对照测试：
   - [`skeleton-target-lower.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-target-lower.mlir)
   - [`skeleton-finalize-memref-to-gpu.mlir`](../../mlir/test/Dialect/Skeleton/skeleton-finalize-memref-to-gpu.mlir)
   - 集成测试：`CPU/test-skeleton-matmul.mlir`（跑通全 pipeline 的权威命令）与 `GPU/test-skeleton-matmul-gpu.mlir`（后者要有 GPU）

### 带着问题读

- 为什么 GPU kernel 不能直接访问 host 内存？（GPU 的地址空间模型：host 内存和 device 内存在物理上分开，kernel 只能读 device 内存）所以 bufferize 之后、生成 kernel 之前，必须把 host memref 换成**设备内存**——这就是 Phase A/B 在做的事。
- **为什么要异步 token**？看注释 13–18 行：`isAsyncWithOneDependency`——这是下游 gpu lowering 对 op 形态的硬性要求。
- 读 Phase A：`gpu.alloc` 的类型怎么构造的？（[`SkeletonTargetLower.cpp:140-142`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonTargetLower.cpp#L140)）→ 它**带不带地址空间**？（不带）
- 读 Phase B 的只读判定（184–190 行）：怎么判断一个 memref 是「只读常量」？（顺着 cast 链找 `memref.get_global`）——get_global 就一定是只读的吗？
- Finalize 那个 pass：`memref::CopyOp` 被标 illegal，那 `gpu.memcpy` 呢？（被标 illegal 单独转换）`gpu.alloc/dealloc` 为什么留 legal？（注释 76–77 行：sync 版本留给 GPU pipeline 处理）

### 读的时候注意（先自己想）

1. **动态 shape 跳过桥接**（[`SkeletonTargetLower.cpp:123-132`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonTargetLower.cpp#L123)）：host memref 有动态维度时，`emitWarning` 然后 `continue`——**桥接被跳过，host memref 直接作为 GPU call 操作数留下**，kernel 会读写 host 内存（静默降级成错误代码）。

2. **设备 memref 类型重建丢信息**（[`SkeletonTargetLower.cpp:135-142`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonTargetLower.cpp#L135) + TODO）：`MemRefType::get(shape, eltType)`——**没带 GPU 地址空间，也丢了 host layout（offset/strides）**，随后 `memref.cast` 回 hostMemrefType 把差异盖住。为什么现在还能跑？（下游 `gpu-lower-to-nvvm` 的 memory-space 分配会补）

3. **单输出假设**（[`SkeletonTargetLower.cpp:208-221`](../../mlir/lib/Dialect/Skeleton/Transforms/SkeletonTargetLower.cpp#L208) + TODO）：只把**最后一个 copy-back 的 host memref** 替换 `call.getResult(0)`（**硬编码下标 0**）；多个可写 memref、多结果、被写过的 global，都会静默丢 copy-back。

4. 把 GPU 桥接整段串起来自己讲一遍：alloc → memcpy(H→D) → call → memcpy(D→H) → dealloc，每一步谁在哪个地址空间、谁持有 token。

### 自测（讲得出来才算过）

- [ ] 不看文件，说出这个 pass 的三个阶段。
- [ ] 说出 GPU 桥接 Phase A 和 Phase B 各做了哪几件事（每步一个 op 名）。
- [ ] 说出设备 memref 类型重建时丢了什么（地址空间 + layout）。
- [ ] 说出动态 shape 时会发生什么（跳过桥接 + host memref 直通 GPU）。
- [ ] 跑测试确认 `skeleton-target-lower.mlir`、`skeleton-finalize-memref-to-gpu.mlir` 通过。

### 这一站的现状与边界（继续写时要知道的）

- 这是**后端最复杂、但已经端到端跑通**的一站（CPU + GPU 集成测试都过）。GPU 桥接的「alloc→memcpy→call→memcpy→dealloc」是真正让异构执行落地的代码。
- 它的边界集中在 GPU bridge 的三个假设：① 静态 shape（动态跳过）；② 地址空间/layout 靠下游补；③ 单输出。CPU 路径（scf.for）没有这些边界。
- 若要支持动态 shape 或多输出的 GPU kernel，改的是 Phase A/B 的 bridge 逻辑；若要搞清楚「为什么地址空间不显式标也能跑」，要追 `gpu-lower-to-nvvm` 的 memory-space 分配。

### 你的复述

（用你自己的话，把这一站讲一遍。讲完你就过关了。）

---

## 10. 站 5 · CIR 前端（最后读，最不完整）

> 读完这站，你该能：**说出 C++ 注解是怎么落到 CIR 的、`CirCallToSkeleton` 手动路径的「收集 → 重写」流程，以及它断在哪、为什么是 no-op。这一站是「继续写」的主战场。**

### 读这些（按顺序）

1. [`CirCallToSkeleton.cpp`](../../clang/lib/CIR/Dialect/Transforms/CirCallToSkeleton.cpp)（384 行）——按 pass 的生命周期读：
   - **收集（Phase 1）**（42–135 行）：`collectPureFunctions`（47 行，读 `skeleton.pure` 属性）、`collectSkeletonOpDecls`（58 行，读 `skeleton.op` 属性）、`extractPureFnRef`（71 行，穿 cir.cast 找 cir.get_global）、`extractPreference`（98 行，位置式解析注解）、`cirTypeToStdType`（114 行，CIR 类型 → MLIR 类型）。
   - **重写（Phase 2）**（140–281 行）：`rewriteToStandardFunc`（166 行，建空 func.func + memref 参数）、`lowerMapCall`（196 行）、`lowerReduceCall`（247 行）。
   - **pass 主逻辑**（287–377 行）：`opDecls.empty()` 就直接 return（297 行）。
2. [`CirLoopToSkeleton.cpp`](../../clang/lib/CIR/Dialect/Transforms/CirLoopToSkeleton.cpp)（116 行）——半自动路径 stub，`getFirstAnnotation`（45 行）读注解，`extractLoopInfo`（60 行）解析，最后只发 warning（105 行）。
3. [`CIRGenStmt.cpp:88-101`](../../clang/lib/CIR/CodeGen/CIRGenStmt.cpp#L88)——`attachLoopAnnotations`：把 C++ 循环语句上的 `AnnotateAttr` 转成 `cir.annotation`，挂到 cir.for/while/do 的 `annotations` 属性。调用点在 1054（for）/1104（doWhile）/1159（while）。
4. 对照测试：[`cir-loop-to-skeleton.cir`](../../clang/test/CIR/Transforms/cir-loop-to-skeleton.cir)

### 带着问题读

- `collectSkeletonOpDecls` 读的是**名为 `skeleton.op` 的直接属性**（63 行）。但 C++ 的 `__attribute__((annotate("skeleton_op", ...)))` 经过 CIR codegen，落到 CIR 上的是 **`annotations` 数组里的 `cir.annotation`**（`#cir.annotation<"skeleton_op", ["map"]>`），不是 `skeleton.op` 直接属性。**对照 §2 契约地图：`skeleton.op`/`skeleton.pure` 只有消费点、没有生产点** → `opDecls` 恒空 → pass 在 297 行直接 return。**这就是「整个手动路径是 no-op」的实证链。**
- `pure_fn` 通过 `SymbolTable::lookupNearestSymbolFrom<func::FuncOp>` 解析（站 1 见过），要求指向 **func.func**。但 `rewriteToStandardFunc` 只重写了包裹函数（`vec_add`），**把 `my_add`（仍是 cir.func）转成 func.func 了吗**？没有 → `pure_fn @my_add not found` 验证失败。
- 函数式风格 vs destination-passing：文档里 `skeleton_map(fn, A, B)` 是**返回值风格**，但 `lowerMapCall` 的 `numInputs = numArgs - 2`（329 行）把 `B` 当成**输出**。`B` 会被当输出缓冲区覆盖——语义错了。
- `extractPreference` 取 `annotations[0].args[0]`（位置式），失配静默默认 `"CPU"`。

### 读的时候注意（先自己想）

1. **手动路径断在哪**：整条链「注解 → `cir.annotation` 属性 → `skeleton.op` 直接属性」之间没有转换环节。你搜代码验证一下「`skeleton.op`/`skeleton.pure` 没有生产点」这个判断，然后想想：要接上这条链，缺的环节是什么？（提示：需要一个把 `annotations` 里的 `cir.annotation` 翻译成 `skeleton.op`/`skeleton.pure` 直接属性、或直接读 `cir.annotation` 的环节。）

2. **`rewriteToStandardFunc` 是有损重建**（161–165 行 TODO 已点明）：所有指针参数 → `memref<?xT>`（shape 丢了），非指针 → index；**函数体整个丢弃**，只塞一个 skeleton op。一个函数多个 skeleton 调用 → 生成多个同名 func.func（符号冲突）；函数里还有非 skeleton 语句 → 静默丢掉。

3. **位置式 memref 参数匹配**（206–210、253 行 TODO）：假设重写函数的 memref 参数顺序 == 调用数据操作数顺序，多一个指针参数或顺序不同就**静默错位绑定**。

4. 对照 `CirLoopToSkeleton`：半自动路径连收集都没有，只有 warning。注意 [`Passes.td:251-273`](../../clang/include/clang/CIR/Dialect/Passes.td#L251) 里它的 description 却宣称「`annotate("skeleton_vector_add", ...)` → `skeleton.vector_add`」——**描述与实现不符**，别信描述。

### 自测（讲得出来才算过）

- [ ] 不看文件，说出 `CirCallToSkeleton` 的收集 → 重写两步各做了什么。
- [ ] 说出「为什么整个手动路径是 no-op」的完整推理链（属性契约 → 无生产点 → opDecls 空 → 提前 return）。
- [ ] 说出 `rewriteToStandardFunc` 至少 3 个有损点。
- [ ] 说出 `CirLoopToSkeleton` 现在实际做了什么。
- [ ] 跑测试：`build/bin/llvm-lit -v build/tools/clang/test/CIR/Transforms/`（含 `cir-loop-to-skeleton.cir`）。

### 这一站的现状与边界（继续写时要知道的）

这是**「继续写」最可能落手的地方**，因为后端已经能跑，前端两条路径都卡在起点。把断点按「生产端 / 消费端」归位：

- **手动路径**：消费端（`CirCallToSkeleton`）已经写了完整的「收集 → 重写」骨架，缺的是**生产端**——把 C++ 注解落到 `skeleton.op`/`skeleton.pure` 直接属性（或改消费端直接读 `cir.annotation`）。此外还缺「纯函数 cir.func → func.func」这一步。接上之后，还要处理 §6.4 的返回值 vs destination-passing 语义矛盾（先拍板）。
- **半自动路径**：生产端（`CIRGenStmt.cpp` 的 `attachLoopAnnotations`）已经通了、注解已经在循环 op 上了，缺的是**消费端**——把 `CirLoopToSkeleton` 从「只发 warning」改成「分析循环体 → 提取纯函数 → 产出 skeleton.map/reduce」。

### 你的复述

（用你自己的话，把这一站讲一遍——尤其讲清楚：如果现在给你一个带 `skeleton_map` 调用的 C++ 程序，这条链会在哪里、因为什么断掉。讲完你就过关了。）

---

## 11. 读完后的验收

全部 5 站讲清楚后，回到 §1–§2，做这几件事：

- [ ] 不看文档，凭记忆画出完整的管线图，标出每一站的 pass 名（CLI 开关）和文件路径。
- [ ] 不看文档，默写 §2 契约地图的 6 个契约项，指出每个「谁生产、谁消费」。
- [ ] 说出每站各有一个什么「边界」：第 1 站的 body 类型矛盾 + 输入 rank 不查、第 2 站的 merge 不查 rank + clone 截断、第 3 站的链式快照脱节、第 4 站的地址空间 + 单输出假设、第 5 站的属性契约断裂。
- [ ] 说出两处断链各自断在「生产端」还是「消费端」，以及要接上各缺什么。
- [ ] 从这些边界里挑一个你判断**现在就要接上/修掉的**，写进你自己的 TODO（是你自己判断，不是背结论）。

**到这一步，这份代码就是你的了。**
